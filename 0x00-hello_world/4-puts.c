#include <stdio.h>
/**
 * main - We are working with puts and not print
 * return: 0 (signifying success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
