#include "main.h"
/**
 * main - function that prints the n times table, starting with 0
 *
 * Return: 0
 */
void print_times_table(int n);
{
	if n >= 0 && n <= 15
		printf("n * n")
	else
		Return(0)
}


