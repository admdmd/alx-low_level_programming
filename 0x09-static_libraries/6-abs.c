#include "main.h"
/**
 * _abs(int)- compute the absolute value of an integer
 * @r : the character
 * Return: r if r is abs, else return -r
 */
int _abs(int r)
{
	if (r > 0)
	{	return (r);
	}
	else
		return (-r);
}
